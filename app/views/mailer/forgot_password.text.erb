ログインID: <%= @user.login %>

<%= @user.name %> 様

旅費清算システムに登録されているアカウントのパスワード再設定の申請を受け付けました。
以下のURLにアクセスして、パスワードを再設定してください。

<%= Setting.url %><%= url_for reset_password_url(login_key: @user.login_key, reset_code: @user.passwords.first.reset_key) %>
<%= Setting.signature %>
