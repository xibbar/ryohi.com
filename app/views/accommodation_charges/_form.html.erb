<%= simple_form_for @accommodation_charge, url: [@company, @accommodation_charge] do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :amount %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
