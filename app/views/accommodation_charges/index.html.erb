<h2><%=t "#{@page_name}.title" %></h2>
<% unless current_user.expired?( @now ) -%>
  <p>
  <%= link_to t('link.new'), [:new, @company, :accommodation_charge], title: t('link.new'), class: 'btn btn-primary' %>
  </p>
<% end -%>


<div class='table-responsive'>
  <table class='table'>
    <tr>
      <th><%=f AccommodationCharge, :name %></th>
      <th><%=f AccommodationCharge, :amount %></th>
      <th></th>
      <% unless current_user.expired?( @now ) -%>
        <th></th>
        <th></th>
      <% end -%>
    </tr>
    <%= render @accommodation_charges %>
  </table>
</div>

<%= link_to t('link.back'), companies_path, class: 'btn btn-secondary' %>
